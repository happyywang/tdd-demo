import React from 'react'
import ReactDOM from 'react-dom/client'
import TDDPresentation from './TDDPresentation.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TDDPresentation />
  </React.StrictMode>,
)